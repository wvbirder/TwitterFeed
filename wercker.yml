box: openjdk
build:
  steps:
    - script:
            name: update apt-get
            code: apt-get update -y
    - script:
            name: install maven
            code: echo Y | apt-get install maven
    - script:
            name: did it work
            code: mvn -version
